[build]
  command   = "npm install"
  functions = "netlify/functions"
  publish   = "dashboard"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_URL"

[[redirects]]
  from   = "/api/*"
  to     = "/.netlify/functions/:splat"
  status = 200
